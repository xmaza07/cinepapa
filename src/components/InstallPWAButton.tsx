
import InstallPWA from './InstallPWA';

// This is a separate component so we can import it in the Navbar component
const InstallPWAButton = () => {
  return <InstallPWA />;
};

export default InstallPWAButton;
